<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StudentAttendanceApp"
        tools:targetApi="31">

        <!-- Activities -->
        <activity
            android:name=".AddClassActivity"
            android:exported="false" />

        <activity
            android:name=".AddStudent"
            android:exported="false" />

        <activity
            android:name=".AddTeachers"
            android:exported="false" />

        <activity
            android:name=".AttendanceActivity"
            android:exported="false" />

        <activity
            android:name=".DashboardActivity"
            android:exported="false" />

        <activity
            android:name=".list_stud"
            android:exported="false" />

        <activity
            android:name=".logout_page"
            android:exported="false" />

        <activity
            android:name=".SignupActivity"
            android:exported="false" />

        <activity
            android:name=".take_attendence"
            android:exported="false" />

        <activity
            android:name=".track_stud"
            android:exported="false" />

        <activity
            android:name=".view_attend"
            android:exported="false" />

        <activity
            android:name=".view_classess"
            android:exported="false" />

        <!-- First Page Activity (Launcher) -->
        <activity
            android:name=".first_pg"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login Activity -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:label="Login"
            android:theme="@style/Theme.StudentAttendanceApp" />

    </application>
</manifest>